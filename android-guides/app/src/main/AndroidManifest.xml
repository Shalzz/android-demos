<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.eightbitlabs.guidesdemo" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="in.eightbitlabs.guidesdemo.BoilerplateApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!--Bugsnag API Key -->
        <!--TODO: add api key-->
        <meta-data android:name="com.bugsnag.android.API_KEY" android:value=""/>

        <activity
            android:name="in.eightbitlabs.guidesdemo.ui.login.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="in.eightbitlabs.guidesdemo.ui.main.MainActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name="in.eightbitlabs.guidesdemo.ui.cart.CartActivity"
            android:label="Cart" >
        </activity>

    </application>

</manifest>
